{
  "name": "@bahi/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate": "prisma db push",
    "migrate:deploy": "prisma db push --accept-data-loss",
    "db:push": "prisma db push",
    "seed": "ts-node prisma/seed.ts",
    "check": "ts-node prisma/check.ts",
    "studio": "prisma studio",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "@bahi/tsconfig": "*",
    "prisma": "^6.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
